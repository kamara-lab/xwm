# xwm.masking

What a JEPA predicts: 2-D blocks, 3-D tubes, temporal splits. Masks are batch-shared `int32` index arrays with static shapes, so a training step compiles once. See [Masking](../concepts/masking.md).

::: xwm.masking
